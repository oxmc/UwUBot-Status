{
  "name": "UwUBot-Status",
  "version": "1.0.1",
  "description": "An auto-updating app to show the status of UwUBot, built with electron-builder.",
  "main": "src/js/main.js",
  "repository": {
    "type": "git",
    "url": "https://github.com/oxmc/UwUBot-Status.git"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "electron .",
    "build-mac": "electron-builder build --mac --publish never",
    "build-win": "electron-builder build --win --publish never",
    "deploy": "electron-builder build --mac --win --publish always"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "electron": "^5.0.1",
    "electron-builder": "^20.40.2"
  },
  "dependencies": {
    "electron-updater": "^4.0.6",
    "node-notifier": "^9.0.1"
  }
}
